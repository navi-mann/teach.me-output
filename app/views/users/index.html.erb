<%= form_for :userCode, :action => users_path, :method => :post, remote: true do |f| %>
<%= f.label :'Write your code here' %>
<%= f.text_area :code, :class => 'codemirror-textarea' %>
<%= submit_tag 'Run', :id => 'run-code' %>
<% end %>
